+function( window, app, _, undefined ){

	// Creating modules
	_.each(app._MODULES, app.createModule);
	delete app._MODULES;

	// Binding DOM


	// Init application user
	
	
}(this, this.app, this._);